<div class="modal-content">
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4 class="modal-title">Delete User in OrgChart</h4>
    </div>
    <div class="modal-body">
        <h3>Are you sure you want to delete user in @ViewBag.Rol?</h3>
        <div class="form-group row">
            <label class="col-md-12 control-label">@ViewBag.User</label>
            <label class="col-md-12 control-label">Turno: @ViewBag.Shift</label>
        </div>


    </div>
    <div class="modal-footer">
        <button type="button" id="btnSave" onclick="deleteUser(id_positionuser=@ViewBag.IdPositionUser, idposition=@ViewBag.IdPosition,idrol = @ViewBag.IdRol)" class="btn buttonSave">Delete</button>
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
    </div>
</div>

<script>
    $('.selectpicker').selectpicker();


</script>
